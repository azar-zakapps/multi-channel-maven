
import { Layout } from "@/components/Layout";
import { ProductTimelineView } from "@/components/ProductTimelineView";

const ProductTimeline = () => {
  return (
    <Layout>
      <ProductTimelineView />
    </Layout>
  );
};

export default ProductTimeline;
